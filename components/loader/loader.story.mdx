import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import Loader from './'
import Icon from '@components/icon'

<Meta title="MDX/Loader" component={Loader} />

export const Template = args => <Loader {...args} />

export const defaultProps = {}

# Loader

Loader component

<br />

<Story
  name="Main"
  args={{
    ...defaultProps,
  }}>
  {Template.bind({})}
</Story>

## White

This variant has white element instead of colored one. This is useful for contrast on darker backgrounds

<Canvas>
  <Story
    name="White"
    args={{
      ...defaultProps,
      white: true,
      size: 'lg',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Sizes

Sizes of loader

- `sm`
- `md`
- `lg`

<br />

<Canvas>
  <Story
    name="Large"
    args={{
      ...defaultProps,
      size: 'lg',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Medium"
    args={{
      ...defaultProps,
      size: 'md',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Small"
    args={{
      ...defaultProps,
      size: 'sm',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Loader} />
