import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import Switch from './'

<Meta title="MDX/Switch" component={Switch} />

export const Template = args => <Switch {...args} />
export const defaultProps = {
  size: 'sm',
  checked: false,
}

# Switch

<br />

<Story
  name="Switch"
  args={{
    ...defaultProps,
  }}>
  {Template.bind({})}
</Story>

<br />

## Sizes

Switch as all the other components uses 3 basic sizes:

- `sm`
- `md`
- `lg`

<br />

<Canvas isColumn>
  <Story
    name="Large"
    args={{
      ...defaultProps,
      size: 'lg',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Medium"
    args={{
      ...defaultProps,
      size: 'md',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Small"
    args={{
      ...defaultProps,
      size: 'sm',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Read only

<Canvas>
  <Story
    name="Read only"
    args={{
      ...defaultProps,
      readOnly: 'true',
      checked: true,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Required

<Canvas>
  <Story
    name="Required"
    args={{
      ...defaultProps,
      required: true,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story
    name="Disabled"
    args={{
      ...defaultProps,
      disabled: true,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Switch} />
