import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import RadioGroup from './'
import Radio from '../radio'

<Meta title="MDX/RadioGroup" component={RadioGroup} />

export const Template = args => (
  <RadioGroup {...args}>
    <Radio label="Option 1" checked/>
    <Radio label="Option 2" />
    <Radio label="Option 3" />
  </RadioGroup>
)

# RadioGroup

<br />

<Story name="RadioGroup">{Template.bind({ label: 'Radio Group' })}</Story>

<br />

## Sizes

RadioGroup as all the other components uses 3 basic sizes:

- `sm`
- `md`
- `lg`

<br />

<Canvas isColumn>
  <Story
    name="Large"
    args={{
      label: 'Large',
      size: 'lg',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Medium"
    args={{
      label: 'Medium',
      size: 'md',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Small"
    args={{
      label: 'Small',
      size: 'sm',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Orientation `Vertical` | `Horizontal`

<Canvas isColumn>
  <Story
    name="Large Horizontal"
    args={{
      label: 'Large',
      orientation: 'horizontal',
      size: 'lg',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Medium Horizontal"
    args={{
      label: 'Medium',
      orientation: 'horizontal',
      size: 'md',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Small Horizontal"
    args={{
      label: 'Small',
      orientation: 'horizontal',
      size: 'sm',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Other states

<br />

<Canvas>
  <Story
    name="Read only"
    args={{
      label: 'Read only',
      readOnly: 'true',
      checked: true,
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Required"
    args={{
      label: 'Required',
      required: true,
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Disabled"
    args={{
      label: 'Disabled',
      disabled: true,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={RadioGroup} />
