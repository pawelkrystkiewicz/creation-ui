import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import Autocomplete from './'
import { Icon } from '@cui/core'

<Meta title='MDX/Autocomplete' component={Autocomplete} />

export const Template = args => <Autocomplete {...args} />
export const options = [
  { id: 1, value: 'Wade Cooper' },
  { id: 2, value: 'Arlene Mccoy' },
  { id: 3, value: 'Devon Webb' },
  { id: 4, value: 'Tom Cook' },
  { id: 5, value: 'Tanya Fox' },
  { id: 6, value: 'Hellen Schmidt' },
]

# Autocomplete

<Story
  name='Autocomplete'
  args={{
    options,
    fullwidth: true,
    label: 'Autocomplete',
  }}
>
  {Template.bind({})}
</Story>

<br />

## Sizes

Autocomplete as all the other components uses 3 basic sizes:

- `sm`
- `md`
- `lg`

<br />

<Canvas isColumn>
  <Story
    name='Large'
    args={{
      options,
      label: 'Large',
      size: 'lg',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Medium'
    args={{
      options,
      label: 'Medium',
      size: 'md',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Small'
    args={{
      options,
      label: 'Small',
      size: 'sm',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Variants

Autocomplete can have different variants that will change their look:

- `contained`
- `outlined`
- `text`

<br />

<Canvas isColumn>
  <Story
    name='Contained'
    args={{
      options,
      label: 'Contained',
      variant: 'contained',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Outlined'
    args={{
      options,
      label: 'Outlined',
      variant: 'outlined',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Text'
    args={{
      options,
      label: 'Text',
      variant: 'text',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Other states

<br />

<Canvas isColumn>
  <Story
    name='Read only'
    args={{
      label: 'Read only',
      readOnly: true,
      checked: true,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Required'
    args={{
      label: 'Required',
      required: true,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Disabled'
    args={{
      label: 'Disabled',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Error'
    args={{
      label: 'Error',
      error: 'Invalid input',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Autocomplete} />
