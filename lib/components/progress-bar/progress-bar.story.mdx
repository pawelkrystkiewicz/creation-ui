import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import ProgressBar from './'

<Meta title="MDX/ProgressBar" component={ProgressBar} />

export const Template = args => <ProgressBar {...args} />
export const defaultProps = {
  size: 'sm',
  value: 50,
}

# ProgressBar

<br />

<Story
  name="ProgressBar"
  args={{
    ...defaultProps,
  }}>
  {Template.bind({})}
</Story>

<br />

## Sizes

ProgressBar as all the other components uses 3 basic sizes:

- `sm`.
- `md`
- `lg`

<br />

<Canvas isColumn>
  <Story
    name="Large"
    args={{
      ...defaultProps,
      size: 'lg',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Medium"
    args={{
      ...defaultProps,
      size: 'md',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Small"
    args={{
      ...defaultProps,
      size: 'sm',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Show value

<Canvas>
  <Story
    name="Show value"
    args={{
      ...defaultProps,
      showValue: true,
      value: 67,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Custom value formatter

<Canvas>
  <Story
    name="Custom formatter"
    args={{
      ...defaultProps,
      showValue: true,
      value: 45,
      formatDisplayValue: v => `Progress: ${v}/100`,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={ProgressBar} />
