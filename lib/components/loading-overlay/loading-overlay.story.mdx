import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import LoadingOverlay from './'
import Icon from '@components/icon'

<Meta title="MDX/LoadingOverlay" component={LoadingOverlay} />

export const Template = args => (
  <div className="relative w-full bg-white shadow-md rounded-lg p-3 overflow-clip">
    <LoadingOverlay {...args} />
    <h3 className="text-blue-500">{args.title || 'Title'}</h3>
    <p className="py-3">
      Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's
      standard dummy text ever since the 1500s, when an unknown printer took a galley of type and like Aldus PageMaker
      including versions of Lorem Ipsum.
    </p>
  </div>
)

export const defaultProps = {
  active: true,
  title: 'LoadingOverlay',
}

# LoadingOverlay

LoadingOverlay component

<br />

<Story
  name="Main"
  args={{
    ...defaultProps,
  }}>
  {Template.bind({})}
</Story>

## Sizes

Sizes of loader

- `sm`
- `md`
- `lg`

<br />

<Canvas>
  <Story
    name="Large"
    args={{
      ...defaultProps,
      size: 'lg',
      title: 'Large',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Medium"
    args={{
      ...defaultProps,
      size: 'md',
      title: 'Medium',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Small"
    args={{
      ...defaultProps,
      size: 'sm',
      title: 'Small',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={LoadingOverlay} />
