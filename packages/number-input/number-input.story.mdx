import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { NumberInput } from './'

<Meta title='MDX/NumberInput' component={NumberInput} />

export const Template = args => <NumberInput {...args} />

export const defaultProps = {
  label: 'NumberInput',
  placeholder: 'Placeholder',
  disabled: false,
  helperText: undefined,
  error: undefined,
  defaultValue: 0,
}

# NumberInput

<br />

<Story
  name='NumberInput'
  args={{
    ...defaultProps,
  }}
>
  {Template.bind({})}
</Story>

<br />

## Sizes

NumberInput as all the other components uses 3 basic sizes:

- `sm`
- `md`
- `lg`

<br />

<Canvas isColumn>
  <Story
    name='Large'
    args={{
      ...defaultProps,
      label: 'Large',
      size: 'lg',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Medium'
    args={{
      ...defaultProps,
      label: 'Medium',
      size: 'md',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Small'
    args={{
      ...defaultProps,
      label: 'Small',
      size: 'sm',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Other states

<br />

<Canvas>
  <Story
    name='Read only'
    args={{
      ...defaultProps,
      label: 'Read only',
      readOnly: 'true',
      checked: true,
      value: 'I am read only',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Required'
    args={{
      ...defaultProps,
      label: 'Required',
      required: true,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Disabled'
    args={{
      ...defaultProps,
      label: 'Disabled',
      placeholder: 'I am disabled, you cannot edit me',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Error'
    args={{
      ...defaultProps,
      label: 'Error',
      error: 'I am error',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='HelperText'
    args={{
      ...defaultProps,
      label: 'With HelperText',
      helperText: 'This is number field',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={NumberInput} />
