import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import {Radio} from './'

<Meta title="MDX/Radio" component={Radio} />

export const Template = args => <Radio {...args} />
export const defaultProps = {
  size: 'sm',
  label: 'Option',
  disabled: false,
  helperText: undefined,
  error: undefined,
}

# Radio

<br/>

<Story
  name="Radio"
  args={{
    ...defaultProps,
  }}>
  {Template.bind({})}
</Story>

<br />

## Sizes

Radio as all the other components uses 3 basic sizes:

- `sm`
- `md`
- `lg`

<br />

<Canvas isColumn>
  <Story
    name="Large"
    args={{
      ...defaultProps,
      label: 'Large',
      size: 'lg',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Medium"
    args={{
      ...defaultProps,
      label: 'Medium',
      size: 'md',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Small"
    args={{
      ...defaultProps,
      label: 'Small',
      size: 'sm',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Other states

<br />

<Canvas>
  <Story
    name="Read only"
    args={{
      ...defaultProps,
      label: 'Read only',
      readOnly: 'true',
      checked: true,
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Required"
    args={{
      ...defaultProps,
      label: 'Required',
      required: true,
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Disabled"
    args={{
      ...defaultProps,
      label: 'Disabled',
      disabled: true,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Radio} />
