import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { Button } from './'
import { Icon } from '@creation-ui/core'

<Meta title="MDX/Button" component={Button} />

export const Template = args => <Button {...args} />

# Button

<Story
  name="Button"
  args={{
    children: 'Click me',
  }}>
  {Template.bind({})}
</Story>

<br />

## Sizes

Button as all the other components uses 3 basic sizes:
- `sm`
- `md`
- `lg`

<br />

<Canvas>
  <Story
    name="Large"
    args={{
      children: 'Large',
      size: 'lg',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Medium"
    args={{
      children: 'Medium',
      size: 'md',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Small"
    args={{
      children: 'Small',
      size: 'sm',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Variants

Button can have different variants that will change their look:

- `contained`
- `outlined`
- `text`

<br />

<Canvas>
  <Story
    name="Contained"
    args={{
      children: 'Contained',
      variant: 'contained',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Outlined"
    args={{
      children: 'Outlined',
      variant: 'outlined',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Text"
    args={{
      children: 'Text',
      variant: 'text',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Icons

Button can have icons set on both left and right ends as well as loading indicator on right (will override icon):

<br />

<Canvas isColumn>
  <Story
    name="Icon Left"
    args={{
      children: 'Icon left',
      iconLeft: <Icon icon="account_circle" />,
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Icon Right"
    args={{
      children: 'Icon right',
      iconRight: <Icon icon="account_circle" />,
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Loading"
    args={{
      children: 'Loading',
      loading: true,
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Loading with icon"
    args={{
      children: 'Loading',
      loading: true,
      iconLeft: <Icon icon="account_circle" />,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Button} />
