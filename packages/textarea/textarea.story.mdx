import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { TextArea } from './'

<Meta title='MDX/TextArea' component={TextArea} />

export const Template = args => <TextArea {...args} />
export const defaultProps = {
  label: 'TextArea',
  placeholder: 'Placeholder',
  disabled: false,
  helperText: undefined,
  invalidTextAreaText: undefined,
}

# TextArea

## TODO

- [ ] Allow to change resize directions by props
- [ ] minRows
- [ ] maxRows

<br />

<Story
  name='TextArea'
  args={{
    ...defaultProps,
  }}
>
  {Template.bind({})}
</Story>

<br />

## Sizes

TextArea as all the other components uses 3 basic sizes:

- `sm`
- `md`
- `lg`

<br />

<Canvas isColumn>
  <Story
    name='Large'
    args={{
      ...defaultProps,
      label: 'Large',
      size: 'lg',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Medium'
    args={{
      ...defaultProps,
      label: 'Medium',
      size: 'md',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Small'
    args={{
      ...defaultProps,
      label: 'Small',
      size: 'sm',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Other states

<br />

<Canvas>
  <Story
    name='Read only'
    args={{
      ...defaultProps,
      label: 'Read only',
      readOnly: 'true',
      checked: true,
      value: 'I am read only',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Required'
    args={{
      ...defaultProps,
      label: 'Required',
      required: true,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Disabled'
    args={{
      ...defaultProps,
      label: 'Disabled',
      placeholder: 'I am disabled, you cannot edit me',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={TextArea} />
